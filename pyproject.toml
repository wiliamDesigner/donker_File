[project]
name = "bookstore"
version = "0.1.0"
description = ""
authors = [
    {name = "WiliamDev",email = "wiliam.omura3@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django (>=6.0.1,<7.0.0)",
    "djangorestframework (>=3.16.1,<4.0.0)",
    "factory-boy (>=3.3.0,<4.0.0)",
    "django-extensions (>=4.1,<5.0)",
    "django-debug-toolbar (>=6.2.0,<7.0.0)",
    "djangorestframework-jwt (>=1.11.0,<2.0.0)"
]

[tool.poetry]
packages = [{include = "bookstore", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
